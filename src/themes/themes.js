import { createTheme } from '@mui/material';

export const theme = createTheme({
  palette: {
    primary: {
      main: '#e6005c',
    },
    secondary: {
      main: '#fff',
    },
  },
  typography: {
    fontFamily: 'Bernard MT Condensed',
  },
});
